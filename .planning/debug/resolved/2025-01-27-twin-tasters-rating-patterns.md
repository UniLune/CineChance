# 2025-01-27 - Twin Tasters: Три Паттерна Совпадения Оценок

## Проблема

Алгоритм Twin Tasters сравнивал только коэффициент корреляции Пирсона, которая не показывает **полную картину совместимости вкусов** при анализе оценок фильмов.

**Нужно было:**
- Показать абсолютные совпадения оценок (одинаковая оценка)
- Показать близкие совпадения (разница ±1, ±2 балла)
- Показать интенсивность вкуса (средняя оценка говорит о том, позитивен ли вкус пользователя)
- Исключить "Брошено" из всех расчётов (это не выражает сравнение вкусов)

## Решение

### 1. Добавлены Константы Порогов Оценок

```typescript
export const RATING_THRESHOLDS = {
  VERY_BAD: { min: 1, max: 3, label: 'Очень плохо', color: '🔴' },
  BAD: { min: 4, max: 5, label: 'Плохо', color: '🟡' },
  NEUTRAL: { min: 6, max: 7, label: 'Нормально', color: '🟢' },
  GOOD: { min: 8, max: 9, label: 'Отлично', color: '🔥' },
  EPIC: { min: 10, max: 10, label: 'Эпик вин!', color: '⚡' },
};
```

### 2. Добавлены Утилиты для Анализа Категорий

**`getRatingCategory(rating)`** — определяет категорию для оценки
**`calculateIntensityMatch(rating1, rating2)`** — расчитывает совпадение интенсивности (0-1)

### 3. Добавлен Новый Интерфейс: RatingMatchPatterns

```typescript
interface RatingMatchPatterns {
  // Паттерн 1: Абсолютные совпадения
  perfectMatches: number;      // Лиловое совпадение оценок
  closeMatches: number;        // Разница ±1 балл
  moderateMatches: number;     // Разница ±2 балла
  
  // Паттерн 2: Проверка категорий
  sameCategory: number;        // Обе в одной категории (1-3, 4-5, 6-7, 8-9, 10)
  differentIntensity: number;  // Разные категории
  
  // Паттерн 3: Интенсивность
  avgRatingUser1: number;      // Средняя оценка пользователя 1
  avgRatingUser2: number;      // Средняя оценка пользователя 2
  intensityMatch: number;      // 0-1, совпадение интенсивности (средних оценок)
  
  // Оригинальное
  pearsonCorrelation: number;  // Pearson correlation
  totalSharedMovies: number;   // Всего общих фильмов
}
```

### 4. Обновлена Функция `computeRatingPatterns()`

Новая функция анализирует **три паттерна совпадения**:

```typescript
async function computeRatingPatterns(
  userIdA: string,
  userIdB: string
): Promise<RatingMatchPatterns>
```

**Что делает:**
1. Находит общие просмотренные фильмы обоих пользователей
2. **Исключает "Брошено"** из анализа (только watched + rewatched)
3. Считает:
   - Абсолютные совпадения (одинаковые оценки)
   - Близкие совпадения (±1, ±2)
   - Совпадения по категориям (1-3 vs 8-9 = разные категории)
4. Рассчитывает среднюю оценку каждого пользователя
5. Определяет совпадение интенсивности

### 5. Обновлена Функция `computeRatingCorrelation()`

Теперь вызывает `computeRatingPatterns()` и возвращает только Pearson корреляцию из результата.

### 6. Обновлена `computeSimilarity()` API

Добавлен опциональный параметр `includePatterns`:

```typescript
export async function computeSimilarity(
  userIdA: string,
  userIdB: string,
  includePatterns?: boolean  // ← NEW
): Promise<SimilarityResult>
```

Если `includePatterns = true`, результат включает детальные паттерны:

```typescript
interface SimilarityResult {
  // ... существующие метрики ...
  ratingPatterns?: RatingMatchPatterns;  // ← NEW
}
```

### 7. Обновлены API Endpoints

**`/api/user/taste-map-comparison/[userId]`:**
- Вызывает `computeSimilarity(currentUserId, comparedUserId, true)` с `includePatterns=true`
- Возвращает `ratingPatterns` в ответе JSON

### 8. Обновлена UI Страница Сравнения

Добавлена новая секция "📊 Анализ совпадения оценок" с:

**Паттерн 1 - Совпадение оценок:**
- ✅ Полное совпадение (зелёная карточка)
- ✅ Близкое совпадение ±1 (жёлтая карточка)
- ✅ Умеренное совпадение ±2 (оранжевая карточка)

**Паттерн 2 - Категория интенсивности:**
- ✅ Одна категория (фиолетовая карточка)
- ✅ Разные категории (красная карточка)

**Паттерн 3 - Интенсивность вкуса:**
- ✅ Вкус пользователя 1 (средняя оценка)
- ✅ Вкус пользователя 2 (средняя оценка)
- ✅ Совпадение интенсивности (визуальный бар 0-100%)
- ✅ Интерпретация результата (текст с эмодзи)

**Информация о корреляции:**
- ✅ Pearson коэффициент
- ✅ Интерпретация (положительная/отрицательная)

## Файлы Изменены

| Файл | Что Изменено |
|------|-------------|
| `src/lib/taste-map/similarity.ts` | Добавлены константы, интерфейсы, функции анализа паттернов |
| `src/app/api/user/taste-map-comparison/[userId]/route.ts` | Добавлен флаг `includePatterns` и возврат `ratingPatterns` |
| `src/app/profile/taste-map/compare/[userId]/page.tsx` | Добавлена UI секция для отображения всех трёх паттернов |

## Трассировка Данных

```
Клик на Twin Taster карточку
  ↓
Навигация на /profile/taste-map/compare/[userId]
  ↓
useEffect: fetch('/api/user/taste-map-comparison/{userId}')
  ↓
API: computeSimilarity(currentUserId, comparedUserId, true)
  ↓
computeSimilarity вызывает computeRatingPatterns()
  ↓
computeRatingPatterns анализирует три паттерна:
  1. Абсолютные совпадения
  2. Категории интенсивности
  3. Средние оценки + intensityMatch
  ↓
Результат с ratingPatterns возвращается JSON
  ↓
UI отображает три паттерна с цветами и числами
```

## Исключение "Брошено"

**Все запросы** в `computeRatingPatterns()` используют:

```typescript
where: {
  userId: someId,
  statusId: { in: COMPLETED_STATUS_IDS },  // [2, 7] = watched + rewatched
}
```

**"Брошено" (statusId=3) полностью исключено**, потому что:
- Указывает на плохой опыт просмотра, а не на вкус
- Может исказить Pearson корреляцию
- Не должно влиять на совметимость вкусов

## Примеры Интерпретации

### Сценарий 1: Очень похожие вкусы
```
Ваша средняя оценка: 7.5
Их средняя оценка: 7.8
Совпадение интенсивности: 95%

Интерпретация: "🔥 Очень похожий вкус - обе стороны смотрят одинаково позитивно"
```

### Сценарий 2: Разные подходы
```
Ваша средняя оценка: 9.0 (Отлично)
Их средняя оценка: 5.5 (Плохо)
Совпадение интенсивности: 25%

Интерпретация: "🟡 Разный подход - одна сторона позитивнее другой"
```

### Сценарий 3: Противоположные вкусы
```
Ваша средняя оценка: 2.0 (Очень плохо)
Их средняя оценка: 9.0 (Отлично)
Совпадение интенсивности: 0%
Pearson корреляция: -0.85 (отрицательная)

Интерпретация: "Смотришь АБСОЛЮТНО противоположные вещи"
```

## Тестирование

На странице `/profile/taste-map/compare/cmkouv7y9000004kvgjzdrnhj`:
1. Должны видеть новую секцию "📊 Анализ совпадения оценок"
2. Три паттерна должны быть видны и информативны
3. Цвета должны соответствовать значениям (зелёный = высокий, красный = низкий)
4. Интерпретация должна быть понятной и полезной

## Дальнейшие Возможности

1. **Экспорт паттернов** — сохранить результаты сравнения
2. **Рекомендации на основе паттернов** — предложить фильмы в категории, где совпадают
3. **История сравнений** — отслеживать изменение совпадений со временем
4. **Коллаборативные рекомендации** — предложить фильмы, которые понравятся обоим

---

**Дата:** 2025-01-27  
**Статус:** IMPLEMENTED & UI READY  
