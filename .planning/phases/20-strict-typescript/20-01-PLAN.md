---
phase: 20
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tsconfig.json
  - .eslintrc.json
  - next.config.ts
  - vitest.config.ts
  - src/lib/taste-map/types.ts
  - src/app/profile/taste-map/TasteMapClient.tsx
  - src/app/api/collection/[id]/route.ts
  - src/lib/taste-map/similarity.ts
  - src/lib/taste-map/person-profile-v2.ts
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
  - QUAL-03
must_haves:
  truths:
    - "TypeScript strict mode enabled (strict, noImplicitAny, strictNullChecks)"
    - "ESLint error-level rules for no-explicit-any and no-unused-vars active"
    - "Next.js no longer ignores build errors"
    - "ActorData and DirectorData types defined and properly imported"
    - "Collection route API id variable correctly scoped in catch block"
    - "RatingMatchPatterns object includes all required properties"
    - "person-profile-v2.ts uses type-safe JSON casting for PersonData[]"
    - "Vitest config compatible with Vitest 4 (coverage under test namespace)"
  artifacts:
    - path: "tsconfig.json"
      provides: "TypeScript configuration with strict flags and es2017 target"
      min_lines: 30
    - path: ".eslintrc.json"
      provides: "ESLint configuration with error-level rules"
      min_lines: 20
    - path: "next.config.ts"
      provides: "Next.js configuration without ignoreBuildErrors"
      min_lines: 10
    - path: "vitest.config.ts"
      provides: "Vitest configuration with coverage under test namespace"
      min_lines: 5
    - path: "src/lib/taste-map/types.ts"
      provides: "Type definitions for ActorData and DirectorData"
      min_lines: 5
    - path: "src/app/profile/taste-map/TasteMapClient.tsx"
      provides: "Proper import and usage of ActorData/DirectorData types"
      min_lines: 1
    - path: "src/app/api/collection/[id]/route.ts"
      provides: "Fixed variable scoping for collectionId in catch block"
      min_lines: 1
    - path: "src/lib/taste-map/similarity.ts"
      provides: "Complete RatingMatchPatterns object with all required properties"
      min_lines: 1
    - path: "src/lib/taste-map/person-profile-v2.ts"
      provides: "Type-safe JSON casting using 'as unknown as' or prisma.$utils.Json.parse"
      min_lines: 4
  key_links:
    - from: "tsconfig.json"
      to: "entire codebase"
      via: "TypeScript compiler options"
      pattern: "\"strict\": true"
    - from: ".eslintrc.json"
      to: "all source files"
      via: "ESLint rules during lint"
      pattern: "\"no-explicit-any\": \"error\""
    - from: "TasteMapClient.tsx"
      to: "src/lib/taste-map/types.ts"
      via: "import statement"
      pattern: "import.*ActorData|DirectorData.*from"
---

<objective>
Phase 20 Goal: Enable strict TypeScript mode and production-grade linting to achieve zero type errors and successful builds.

This first plan updates core configurations and fixes the most critical type errors that block strict mode compilation. It sets the foundation for systematic any elimination.

**Purpose:** Upgrade code quality infrastructure (TypeScript + ESLint) and resolve known type issues that would otherwise flood the error log.

**Output:**
- tsconfig.json with strict mode + es2017 target
- .eslintrc.json with error-level no-explicit-any and no-unused-vars
- next.config.ts without ignoreBuildErrors
- vitest.config.ts fixed for Vitest 4
- New ActorData/DirectorData interfaces
- Fixed collection route, RatingMatchPatterns, and JSON casting
</objective>

<execution_context>
@C:/Users/n0rds/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/n0rds/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-strict-typescript/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update TypeScript configuration to strict mode</name>
  <files>
    tsconfig.json
  </files>
  <action>
    Edit tsconfig.json:
    1. Change compilerOptions.target from "es5" to "es2017"
    2. Set compilerOptions.strict to true
    3. Set compilerOptions.noImplicitAny to true
    4. Set compilerOptions.strictNullChecks to true
    5. Keep existing options (esModuleInterop, moduleResolution, etc.)
    
    Rationale: es2017 target enables native Map/Set iteration; strict mode enforces extensive type checking.
  </action>
  <verify>
    Run: npx tsc --noEmit 2>&1 | head -5
    Expected: No config errors; type errors appear but include Map/Set iteration errors resolved.
  </verify>
  <done>
    tsconfig.json updated with:
    - "target": "es2017"
    - "strict": true
    - "noImplicitAny": true
    - "strictNullChecks": true
  </done>
</task>

<task type="auto">
  <name>Task 2: Update ESLint and Next.js/Vitest configs</name>
  <files>
    .eslintrc.json
    next.config.ts
    vitest.config.ts
  </files>
  <action>
    a) .eslintrc.json:
       - Change rule "@typescript-eslint/no-explicit-any": "off" → "error"
       - Change rule "no-unused-vars": "off" → "error"
       - Change rule "@typescript-eslint/no-unused-vars": "off" → "error" (or remove to use base rule)
    
    b) next.config.ts:
       - In typescript block, set ignoreBuildErrors: false (or delete the entire typescript block to use Next.js default)
    
    c) vitest.config.ts:
       - Move the `coverage` object inside the `test` namespace:
         ```typescript
         export default defineConfig({
           test: {
             environment: 'node',
             globals: true,
             include: ['src/lib/__tests__/**/*.test.ts'],
             testTimeout: 30000,
             coverage: {  // Move here
               provider: 'v8',
               reporter: ['text', 'json', 'html'],
               thresholds: { lines: 80, functions: 80, branches: 70, statements: 80 }
             }
           },
           resolve: { alias: { '@': path.resolve(__dirname, './src') } }
         });
         ```
  </action>
  <verify>
    Run: npm run lint -- --no-ignore 2>&1 | head -10
    Expected: Lint runs, shows errors but no config errors.
  </verify>
  <done>
    - ESLint rules set to error for any and unused vars
    - next.config.ts no longer ignores build errors
    - Vitest config structure corrected
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix taste-map module critical type errors</name>
  <files>
    src/lib/taste-map/types.ts
    src/app/profile/taste-map/TasteMapClient.tsx
    src/app/api/collection/[id]/route.ts
    src/lib/taste-map/similarity.ts
    src/lib/taste-map/person-profile-v2.ts
  </files>
  <action>
    a) Add missing ActorData and DirectorData interfaces in src/lib/taste-map/types.ts:
       ```typescript
       export interface ActorData {
         tmdbPersonId: number;
         name: string;
         avgWeightedRating: number;
         count: number;
       }
       
       export interface DirectorData {
         tmdbPersonId: number;
         name: string;
         avgWeightedRating: number;
         count: number;
       }
       ```
    
    b) In src/app/profile/taste-map/TasteMapClient.tsx:
       - Add import: `import type { ActorData, DirectorData } from '@/lib/taste-map/types';`
       - The existing type annotations in .map() will then recognize ActorData and DirectorData.
    
    c) In src/app/api/collection/[id]/route.ts:
       - At top of GET function after `export async function GET(req: Request, { params }: { params: { id: string } })`:
         add: `const collectionId = params.id;`
       - In the catch block line 134, change `collectionId: id` to `collectionId: collectionId`
    
    d) In src/lib/taste-map/similarity.ts around line 518:
       - Find the object literal assigned to the RatingMatchPatterns return type
       - Add the missing properties with computed or placeholder values:
         * largeDifference: number → compute count of ratings with |diff| > 3
         * avgRatingDifference: number → average absolute difference
         * positiveRatingsPercentage: number → % of ratings >= 6
         * bothRewatchedCount: number → count of movies both users rewatched
         * overallMovieMatch: number → overall match score (could be pearsonCorrelation or computed)
       - If computation is not trivial, set to 0 for now with a TODO comment.
    
    e) In src/lib/taste-map/person-profile-v2.ts at lines 149, 157, 201, 293:
       - Replace assignments like:
         ```typescript
         topActors: data.topPersons as PersonData[],
         ```
       - With type-safe assertion:
         ```typescript
         topActors: data.topPersons as unknown as PersonData[],
         ```
       - Or using Prisma utility:
         ```typescript
         topActors: prisma.$utils.Json.parse<PersonData[]>(data.topPersons),
         ```
       - Apply same pattern to topDirectors and any other PersonData[] casts.
    
    Note: The Map/Set iteration errors in compute.ts and similarity.ts (TS2802) will be automatically resolved by the es2017 target set in Task 1; no code changes required for those.
  </action>
  <verify>
    Run: npx tsc --noEmit 2>&1 | grep "error TS" | wc -l
    Compare with baseline before this plan (should have ~13 errors). After this plan, the specific 13 errors should be gone:
    - vitest config error
    - Map/Set iteration (3 instances) - fixed by target es2017
    - JSON casting (4 instances) - fixed by type-safe casts
    - ActorData/DirectorData missing - fixed by new interfaces
    - Collection route id undefined - fixed by scoped variable
    - RatingMatchPatterns missing props - fixed by adding them
    Expected remaining: ~187 any + unused vars errors (but note ESLint now errors on them too).
  </verify>
  <done>
    - ActorData/DirectorData interfaces created and imported
    - Collection route id scope corrected
    - RatingMatchPatterns object complete
    - person-profile-v2.ts JSON casts type-safe
    - Zero of the original 13 TypeScript errors remain
  </done>
</task>

</tasks>

<verification>
**Phase 20 verification progress (after Plan 01):**

Run these commands and record counts:

1. `npx tsc --noEmit` → record total errors (expect ~185)
2. `npm run lint` → record ESLint errors (expect many for any/unused)
3. `git status --short` → verify modified files match those in this plan

**Expected outcome:** Configuration strictness enforced, critical blockers resolved, but many any/unused violations remain (to be addressed in Plan 02).

**Do NOT proceed to user testing yet** - Phase not complete until Plan 03.
</verification>

<success_criteria>
- Configuration files meet production-grade standards
- All pre-existing blocking type errors (13) are fixed
- TypeScript compiles with ~185 errors (any/unused only, no config or missing-type errors)
- ESLint runs with strict rules
- Codebase ready for systematic any elimination (Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/20-strict-typescript/20-01-SUMMARY.md`
</output>
