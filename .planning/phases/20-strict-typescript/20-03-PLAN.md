---
phase: 20
plan: 03
type: execute
wave: 3
depends_on:
  - "20-02"
files_modified:
  - src/**/*.ts
  - src/**/*.tsx
  - .eslintrc.json
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
  - QUAL-03
must_haves:
  truths:
    - "TypeScript compilation succeeds with zero errors (npx tsc --noEmit exits 0)"
    - "Production build completes successfully (npm run build exits 0)"
    - "ESLint passes with zero warnings (npm run lint --max-warnings 0 exits 0)"
    - "Test suite passes (npm run test:ci exits 0)"
    - "All unused variables eliminated or properly prefixed"
    - "No remaining strict null check issues requiring ts-expect-error"
  artifacts:
    - path: "tsconfig.json"
      provides: "Strict TypeScript configuration"
      min_lines: 30
    - path: ".eslintrc.json"
      provides: "Strict ESLint configuration"
      min_lines: 20
    - path: "next.config.ts"
      provides: "Build configuration respecting type errors"
      min_lines: 10
  key_links:
    - from: "npx tsc --noEmit"
      to: "0 exit code"
      via: "strict type checking"
      pattern: "exit code 0"
    - from: "npm run build"
      to: "successful compilation"
      via: "Next.js production build"
      pattern: "Compiled successfully"
---

<objective>
Finalize strict TypeScript mode by ensuring zero type errors, successful build, and passing tests.

**Purpose:** Verify that all type issues are resolved and the codebase meets production-grade quality standards.

**Output:** 
- `npx tsc --noEmit` passes (exit 0)
- `npm run build` succeeds
- `npm run lint` passes with no warnings
- `npm run test:ci` passes
- All manual smoke tests pass

This plan includes final cleanup of any remaining issues (unused vars, null checks) and comprehensive verification.
</objective>

<execution_context>
@C:/Users/n0rds/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/n0rds/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-strict-typescript/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix remaining strictNullChecks issues</name>
  <files>
    src/**/*.ts
    src/**/*.tsx
  </files>
  <action>
    Run: `npx tsc --noEmit 2>&1 | grep "error TS"` to identify any remaining errors.
    
    Focus on strictNullChecks errors (e.g., "Object is possibly 'null'", "Object is possibly 'undefined'").
    For each error:
    - Add null/undefined checks: `if (value != null) { ... }`
    - Use optional chaining: `value?.property`
    - Use nullish coalescing: `value ?? default`
    - If the value comes from a source that guarantees non-null, use a non-null assertion: `value!` (sparingly)
    
    Iterate until `npx tsc --noEmit` shows 0 errors.
    
    Note: If any errors are from external library typings that cannot be fixed, consider adding `// @ts-expect-error` with a comment, but aim for zero without expect-errors.
  </action>
  <verify>
    Run: `npx tsc --noEmit`
    Expected: exit code 0, no error output
  </verify>
  <done>
    - All strictNullChecks violations resolved
    - TypeScript compiles cleanly with strict mode
  </done>
</task>

<task type="auto">
  <name>Task 2: Final ESLint cleanup and build verification</name>
  <files>
    src/**/*.ts
    src/**/*.tsx
  </files>
  <action>
    a) Run ESLint to catch any remaining issues:
       `npm run lint -- --max-warnings 0`
       If any warnings appear, fix them (unused vars, any, etc.)
    
    b) Run production build:
       `npm run build`
       If build fails due to type errors, return to Task 1.
       If build fails due to other reasons (runtime, missing env), document but do not block phase completion if type errors are zero.
    
    c) Run test suite:
       `npm run test:ci`
       Ensure all tests pass. If tests fail due to strict mode changes, fix test code (tests should be typed as well).
  </action>
  <verify>
    - `npm run lint --max-warnings 0` exits with code 0
    - `npm run build` exits with code 0
    - `npm run test:ci` exits with code 0
  </verify>
  <done>
    - ESLint passes with no warnings
    - Production build succeeds
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 3: Manual smoke testing</name>
  <files>
    src/app/page.tsx
    src/app/recommendations/page.tsx
    src/app/profile/page.tsx
    src/app/search/page.tsx
    src/app/api/**/*.ts
  </files>
  <action>
    Start the development server: `npm run dev`
    
    Manually test critical user flows:
    1. Homepage loads without errors (check console for type-related runtime errors)
    2. Recommendations page loads and displays recommendations
    3. Search functionality works (search for a movie, see results)
    4. Profile page loads with user stats and taste map (if applicable)
    5. Watchlist operations (add to list, change status, rate) work
    6. Authentication flows (login, logout, signup) work if applicable
    
    Also check:
    - No 500 errors in API routes (use browser devtools Network tab)
    - No console type errors (red boxes)
    - Pages render without TypeScript runtime errors
    
    If any runtime errors related to type changes are found, return to Tasks 1-2 to fix.
  </action>
  <verify>
    - All tested pages load successfully
    - No TypeScript runtime errors in browser console
    - API calls return expected data (200 status)
    - User interactions work as expected
  </verify>
  <done>
    - Critical user flows verified working
    - No type-related runtime errors observed
    - Phase 20 objectives fully achieved
  </done>
</task>

</tasks>

<verification>
**Final Phase 20 verification checklist:**

- [x] `npx tsc --noEmit` exits 0
- [x] `npm run lint --max-warnings 0` exits 0
- [x] `npm run build` exits 0
- [x] `npm run test:ci` exits 0
- [x] Manual smoke tests pass (critical pages load, no runtime type errors)
- [x] All any type usages eliminated
- [x] All unused variables addressed
- [x] Config files reflect strict mode and production-grade linting
- [x] Git changes committed with descriptive messages

If all above pass, Phase 20 is complete.
</verification>

<success_criteria>
**Phase 20 Success Criteria (all must be true):**
1. TypeScript strict mode fully enabled (`strict`, `noImplicitAny`, `strictNullChecks` all true)
2. ESLint error rules for `no-explicit-any` and `no-unused-vars` active and passing
3. Next.js does not ignore build errors (`ignoreBuildErrors` removed/false)
4. Zero TypeScript compilation errors (`npx tsc --noEmit` exits 0)
5. Production build succeeds (`npm run build` exits 0)
6. Lint passes with zero warnings (`npm run lint --max-warnings 0` exits 0)
7. Test suite passes (`npm run test:ci` exits 0)
8. Manual verification confirms no regression in critical user flows
</success_criteria>

<output>
After completion, create `.planning/phases/20-strict-typescript/20-03-SUMMARY.md`
</output>
