---
phase: 21-serial-numbers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/app/components/MovieCard.tsx, src/app/components/FilmGridWithFilters.tsx]
autonomous: true
requirements: [SERIAL-01]
must_haves:
  truths:
    - "Order numbers (1, 2, 3...) are visible on movie cards in 'Мои фильмы'"
    - "Numbers are small, pale golden, and maximally unobtrusive"
    - "Numbers do not overlap with status icons"
    - "User can see the last number and verify against database count"
  artifacts:
    - path: "src/app/components/MovieCard.tsx"
      provides: "Order number rendering on card"
      min_lines: 10
    - path: "src/app/components/FilmGridWithFilters.tsx"
      provides: "Passing index prop to MovieCard"
      min_lines: 1
  key_links:
    - from: "src/app/components/FilmGridWithFilters.tsx"
      to: "src/app/components/MovieCard.tsx"
      via: "prop drilling index={index}"
      pattern: "index={index}"
---

<objective>
Add order numbers (1, 2, 3...) to each movie card in 'Мои фильмы' (My Movies) page, visible in all tabs.

Purpose: Allow users to verify the total count of loaded results by seeing the last number (e.g., if last card shows "10", user can verify there are 10 movies in that tab).
Output: Modified MovieCard and FilmGridWithFilters components.
</objective>

<execution_context>
@C:/Users/n0rds/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/n0rds/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-serial-numbers/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update MovieCard Component to Support Order Numbers</name>
  <files>src/app/components/MovieCard.tsx</files>
  <action>
    1. Add `index?: number` to the `MovieCardProps` interface.
    2. In the component JSX, conditionally render the order number if `index` is provided.
    3. Use styling: `absolute top-2 left-2 z-20 bg-amber-900/40 text-amber-100/90 text-xs px-1.5 py-0.5 rounded shadow-sm`.
    4. Display value: `index + 1`.
  </action>
  <verify>Check that the file compiles without TypeScript errors.</verify>
  <done>MovieCard accepts `index` prop and renders the number in the top-left corner.</done>
</task>

<task type="auto">
  <name>Task 2: Pass Index Prop from FilmGridWithFilters</name>
  <files>src/app/components/FilmGridWithFilters.tsx</files>
  <action>
    1. Locate the `map` function rendering `MovieCard`.
    2. Pass `index={index}` to the `MovieCard` component.
  </action>
  <verify>Check that the file compiles without TypeScript errors.</verify>
  <done>FilmGridWithFilters passes the index prop to MovieCard.</done>
</task>

</tasks>

<verification>
Run `npm run lint` to ensure no TypeScript or ESLint errors.
Run `npm run test:ci` to verify no regressions.
</verification>

<success_criteria>
- Movie cards display order numbers (1, 2, 3...) on the "Мои фильмы" page.
- Numbers are styled as small, pale gold text in the top-left corner.
- Numbers do not overlap with status icons.
- All tabs in "Мои фильмы" show numbers.
- User can see the last number to verify against database count.
- ESLint passes with 0 errors.
- All existing tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/21-serial-numbers/21-01-SUMMARY.md`
</output>
