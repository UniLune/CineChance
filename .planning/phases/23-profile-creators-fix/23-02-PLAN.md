---
phase: 23-profile-creators-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/app/profile/creators/CreatorsClient.tsx]
autonomous: true
requirements: []

must_haves:
  truths:
    - "CreatorsClient has logger import matching ActorsClient"
    - "CreatorsClient uses CineChance logo for rating display (matching actors pattern)"
    - "CreatorsClient has type assertion for API response"
  artifacts:
    - path: "src/app/profile/creators/CreatorsClient.tsx"
      provides: "Creators client component with parity to ActorsClient"
      contains: "logger"
  key_links:
    - from: "src/app/profile/creators/CreatorsClient.tsx"
      to: "/api/user/achiev_creators"
      via: "fetch"
      pattern: "fetch.*achiev_creators"
---

<objective>
Fix CreatorsClient.tsx to match ActorsClient.tsx behavior - add logger, fix type assertion, use CineChance logo for rating.

Purpose: Client-side parity between creators and actors pages.
Output: Updated CreatorsClient.tsx with logger, type assertion, and CineChance logo rating display.
</objective>

<context>
# Reference: actors client (working)
@src/app/profile/actors/ActorsClient.tsx

# Current broken: creators client
@src/app/profile/creators/CreatorsClient.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add logger import and debug logging</name>
  <files>src/app/profile/creators/CreatorsClient.tsx</files>
  <action>
Add logger import to match ActorsClient pattern:

1. **Add import at top of file (after existing imports):**
```typescript
import { logger } from '@/lib/logger';
```

2. **Add debug logging after API response (after line 91):**
After `const data = await response.json();`, add:
```typescript
logger.debug('Creators API response', { data });

if (progressIntervalRef.current) {
  clearInterval(progressIntervalRef.current);
}

const creatorsData = data.creators || [];
logger.debug('Creators data received', { count: creatorsData.length });

// Log each creator's progress for debugging
creatorsData.forEach((creator: CreatorAchievement, index: number) => {
  logger.debug('Creator progress', { index: index + 1, name: creator.name, watched: creator.watched_movies, total: creator.total_movies, progress: creator.progress_percent });
});
```

Per user decision: "Add logger import and debug logging statements to CreatorsClient.tsx (matching ActorsClient.tsx)"
  </action>
  <verify>
Grep for "logger" in CreatorsClient.tsx - should have import and debug statements
  </verify>
  <done>CreatorsClient has logger import and debug logging matching ActorsClient</done>
</task>

<task type="auto">
  <name>Task 2: Add type assertion to API response</name>
  <files>src/app/profile/creators/CreatorsClient.tsx</files>
  <action>
Add type assertion to the API response parsing:

**Current (line 91):**
```typescript
const data = await response.json();
```

**Replace with:**
```typescript
const data = await response.json() as { creators: CreatorAchievement[] };
```

This matches the actors pattern (line 99 in ActorsClient.tsx):
```typescript
const data = await response.json() as { actors: ActorAchievement[] };
```

Per user decision: "Add type assertion to CreatorsClient API response: as { creators: CreatorAchievement[] }"
  </action>
  <verify>
Grep for "as { creators:" in CreatorsClient.tsx
  </verify>
  <done>API response has proper type assertion</done>
</task>

<task type="auto">
  <name>Task 3: Use CineChance logo for rating display</name>
  <files>src/app/profile/creators/CreatorsClient.tsx</files>
  <action>
Replace the star icon rating display with CineChance logo (matching actors pattern):

**Current (lines 313-320):**
```typescript
{creator.average_rating !== null && (
  <span className="text-blue-400 text-xs flex items-center gap-0.5">
    <svg className="w-3 h-3" viewBox="0 0 24 24" fill="currentColor">
      <path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/>
    </svg>
    {creator.average_rating}
  </span>
)}
```

**Replace with (matching actors pattern lines 351-365):**
```typescript
{creator.average_rating !== null && (
  <div className="flex items-center bg-gray-800/50 rounded text-sm flex-shrink-0">
    <div className="w-5 h-5 relative mx-1">
      <Image 
        src="/images/logo_mini_lgt.png" 
        alt="CineChance Logo" 
        fill 
        className="object-contain" 
      />
    </div>
    <span className="text-gray-200 font-medium pr-2">
      {creator.average_rating.toFixed(1)}
    </span>
  </div>
)}
```

Note: Need to add `import Image from 'next/image';` if not already present (it is present at line 5).

Per user decision: "Use CineChance logo for average rating display (matching actors pattern)"

Keep the blue color scheme intentionally (amber for actors, blue for creators - visual differentiation per user decision).
  </action>
  <verify>
Grep for "logo_mini_lgt.png" in CreatorsClient.tsx
  </verify>
  <done>Rating display uses CineChance logo</done>
</task>

</tasks>

<verification>
- [ ] Logger import present
- [ ] Debug logging statements present
- [ ] Type assertion on API response
- [ ] CineChance logo used for rating display
- [ ] Build passes with no TypeScript errors
</verification>

<success_criteria>
CreatorsClient matches ActorsClient pattern: has logger, type assertion, and CineChance logo rating</success_criteria>

<output>
After completion, create `.planning/phases/23-profile-creators-fix/23-02-SUMMARY.md`
</output>
