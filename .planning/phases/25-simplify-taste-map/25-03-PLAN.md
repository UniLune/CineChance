---
phase: 25-simplify-taste-map
plan: '03'
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/profile/taste-map/TasteMapClient.tsx
  - .planning/phases/24-taste-map-db-read/tdd/spec-24-01-client.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - TasteMapClient renders without genre bar chart block
    - TasteMapClient renders without rating distribution pie chart block
    - TasteMap page still shows summary stats, computed metrics, behavior profile, and TwinTasters
    - No recharts imports remain in TasteMapClient
    - Test expects genre profile block to NOT be present
  artifacts:
    - path: src/app/profile/taste-map/TasteMapClient.tsx
      provides: Client component without visualization charts
      contains: No recharts imports
      not_contains: BarChart, PieChart, ResponsiveContainer, genreData, ratingData, typeData, COLORS
    - path: .planning/phases/24-taste-map-db-read/tdd/spec-24-01-client.test.tsx
      provides: Updated test expectations
      not_contains: "toContain('Профиль жанров')"
  key_links:
    - from: src/app/profile/taste-map/TasteMapClient.tsx
      to: recharts
      via: imports
      pattern: from 'recharts'
---

<objective>
Remove genre bar chart and rating pie chart visualization blocks from TasteMapClient, along with all supporting imports and variables.

Purpose: Simplify the taste-map page per phase 25 goal — keep only summary stats, computed metrics, behavior profile, and TwinTasters.
Output: Cleaner TasteMapClient.tsx (~50 lines smaller), updated test expectations.
</objective>

<execution_context>
@C:/Users/n0rds/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/app/profile/taste-map/TasteMapClient.tsx
@.planning/phases/24-taste-map-db-read/tdd/spec-24-01-client.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Remove chart blocks and dead code from TasteMapClient</name>
  <files>src/app/profile/taste-map/TasteMapClient.tsx</files>
  <action>
    Remove the following from TasteMapClient.tsx:

    1. Line 3 — Remove entire recharts import line:
       `import { BarChart, Bar, XAxis, YAxis, ResponsiveContainer, Tooltip, PieChart, Pie, Cell, Legend } from 'recharts';`

    2. Lines 12-19 — Remove COLORS constant (only used by ratingData/typeData, both being removed)

    3. Lines 46-52 — Remove genreData block:
       ```
       const genreEntries = Object.entries(tasteMap.genreProfile)
         .sort(([, a], [, b]) => b - a)
         .slice(0, 10);
       const genreData = genreEntries.map(([name, value]) => ({
         name: name.charAt(0).toUpperCase() + name.slice(1),
         value,
       }));
       ```

    4. Lines 55-59 — Remove ratingData variable

    5. Lines 62-67 — Remove typeData variable (already unused)

    6. Lines 99-129 — Remove Genre Profile horizontal bar chart JSX block:
       `{/* Genre Profile - Horizontal Bar Chart */}` through closing `</div>`

    7. Lines 131-164 — Remove Rating Distribution pie chart JSX block:
       `{/* Rating Distribution - Pie Chart */}` through closing `</div>`

    Keep intact:
    - Summary Stats grid (lines 72-97)
    - Computed Metrics Details (lines 166-207)
    - Behavior Profile (lines 209-244)
    - TwinTasters component (line 247)
    - Empty state (lines 25-43)
  </action>
  <verify>npm run lint passes, TypeScript compiles without errors</verify>
  <done>TasteMapClient has no recharts imports, no chart blocks, and renders without genre/pie visualizations.</done>
</task>

<task type="auto">
  <name>Update test expectations for removed genre profile block</name>
  <files>.planning/phases/24-taste-map-db-read/tdd/spec-24-01-client.test.tsx</files>
  <action>
    Update the test at lines 37-49:

    Change from:
    ```typescript
    it('should render genre profile section', () => {
      const html = renderToStaticMarkup(
        <TasteMapClient
          tasteMap={createMockTasteMap({
            genreProfile: { Action: 30, Drama: 25, Comedy: 20 },
          })}
          userId="user-123"
        />
      );

      expect(html).toContain('Профиль жанров');
      expect(html).toContain('recharts');
    });
    ```

    Change to:
    ```typescript
    it('should not render genre profile section (removed in phase 25)', () => {
      const html = renderToStaticMarkup(
        <TasteMapClient
          tasteMap={createMockTasteMap({
            genreProfile: { Action: 30, Drama: 25, Comedy: 20 },
          })}
          userId="user-123"
        />
      );

      expect(html).not.toContain('Профиль жанров');
    });
    ```
  </action>
  <verify>npm run test:ci passes</verify>
  <done>Test expects genre profile block to NOT be present in rendered output.</done>
</task>

</tasks>

<verification>
- Run: npm run lint
- Run: npm run test:ci
- Verify TasteMap page at /profile/taste-map loads without bar chart or pie chart blocks
</verification>

<success_criteria>
- TasteMapClient.tsx: No recharts imports, no chart JSX blocks, no COLORS/genreData/ratingData/typeData variables
- Test file: Genre profile test updated to expect absence
- All remaining sections visible: summary stats, metrics, behavior profile, TwinTasters
- Empty state still works when no genre data
</success_criteria>

<output>
After completion, create `.planning/phases/25-simplify-taste-map/25-03-SUMMARY.md`
</output>
