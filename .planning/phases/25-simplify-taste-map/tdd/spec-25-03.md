# Unit Spec: Phase 25-03 — TasteMapClient Chart Removal

## Target Component

`src/app/profile/taste-map/TasteMapClient.tsx`

## Changes Required

### Imports
- **Remove:** `import { BarChart, Bar, XAxis, YAxis, ResponsiveContainer, Tooltip, PieChart, Pie, Cell, Legend } from 'recharts';`
- **Keep:** `import type { TasteMap } from '@/lib/taste-map/types';`
- **Keep:** `import TwinTasters from './TwinTasters';`

### Variables to Remove
- `COLORS` constant (lines 12-19) — only used by ratingData and typeData
- `genreEntries` and `genreData` (lines 46-52) — data for bar chart
- `ratingData` (lines 55-59) — data for pie chart
- `typeData` (lines 62-67) — already unused

### JSX Blocks to Remove
- Genre Profile bar chart block (lines 99-129)
- Rating Distribution pie chart block (lines 131-164)

### JSX Blocks to Keep
- Summary Stats grid (lines 71-97)
- Computed Metrics Details (lines 166-207)
- Behavior Profile (lines 209-244)
- TwinTasters component (line 247)
- Empty state (lines 24-43)

## Test Updates

`spec-24-01-client.test.tsx`:
- Change test 'should render genre profile section' to expect genre profile NOT present
- Remove assertion `expect(html).toContain('recharts')`
- Rename test to 'should not render genre profile section (removed in phase 25)'

## Verification
- `npm run lint` passes
- `npm run test:ci` passes (all 281+ tests including new acceptance tests)
- TypeScript compiles without errors
