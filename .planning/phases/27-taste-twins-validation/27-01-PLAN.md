---
phase: 27
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/taste-map/similarity.ts
  - src/app/api/user/similar-users/route.ts
  - src/lib/taste-map/similarity-storage.ts
autonomous: true
requirements:
  - VALID-01
  - VALID-02
must_haves:
  truths:
    - "Only users with ≥3 completed movies (watched/rewatched) appear as similar users"
    - "Similarity match threshold is ≥40% (overallMatch >= 0.4)"
    - "TwinTasters block shows no users with 0 watch count or <40% match"
  artifacts:
    - path: "src/lib/taste-map/similarity.ts"
      provides: "isSimilar() threshold change from >0.1 to >=0.4"
      contains: "isSimilar(result: SimilarityResult): result.overallMatch >= 0.4"
    - path: "src/app/api/user/similar-users/route.ts"
      provides: "Filtering of similar users by watch count and match percentage"
      contains: "filter(u => u.watchCount >= 3 && u.overallMatch >= 40)"
    - path: "src/lib/taste-map/similarity-storage.ts"
      provides: "getUserCompletedWatchCount function for efficient counting"
      exports: ["getUserCompletedWatchCount"]
  key_links:
    - from: "src/app/api/user/similar-users/route.ts"
      to: "src/lib/taste-map/similarity-storage.ts"
      via: "getUserCompletedWatchCount for enrichment"
      pattern: "getUserCompletedWatchCount"
    - from: "src/app/api/user/similar-users/route.ts"
      to: "src/lib/taste-map/similarity.ts"
      via: "isSimilar threshold"
      pattern: "isSimilar.*>=.*0\\.4"
---

<objective>
Filter and validate similar users to show only legitimate taste twins with sufficient history and high match percentage.

Purpose: Prevent showing users with 0 movies or low match (<40%), ensure data quality in TwinTasters block.
Output: Updated similarity threshold (40%), watch count filtering (≥3 completed), and enrichment using only completed statuses.
</objective>

<execution_context>
@C:/Users/n0rds/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/n0rds/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-taste-twins-validation/RESEARCH.md
@src/lib/taste-map/similarity.ts
@src/app/api/user/similar-users/route.ts
@src/lib/taste-map/similarity-storage.ts
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Raise isSimilar threshold to 40%</name>
  <files>
    src/lib/taste-map/similarity.ts
  </files>
  <action>
    Change the isSimilar function threshold from `> 0.1` to `>= 0.4` (40%).
    Also add a named constant MIN_MATCH_THRESHOLD = 0.4 for configurability.
    Update any inline comments referencing the old threshold.
  </action>
  <verify>
    grep -n "isSimilar" src/lib/taste-map/similarity.ts shows `>= 0.4` and MIN_MATCH_THRESHOLD defined.
  </verify>
  <done>
    isSimilar(result) returns true only when overallMatch >= 0.4.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add getUserCompletedWatchCount helper</name>
  <files>
    src/lib/taste-map/similarity-storage.ts
  </files>
  <action>
    Implement `getUserCompletedWatchCount(userIds: string[])` that returns a Map<userId, count>.
    Use Prisma.groupBy to count watchList entries with statusId in COMPLETED_STATUS_IDS for all userIds in one query.
    Avoid N+1 queries during enrichment.
  </action>
  <verify>
    Function exists, uses groupBy, filters by COMPLETED_STATUS_IDS, returns Map.
  </verify>
  <done>
    Can fetch completed watch counts for multiple users efficiently.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update similar-users API enrichment and filtering</name>
  <files>
    src/app/api/user/similar-users/route.ts
  </files>
  <action>
    - Replace `userInfoById.get(u.userId)?.watchList.length` with call to `getUserCompletedWatchCount([u.userId])`.
    - After building enrichedResults, add filter: `filter(u => u.watchCount >= 3 && u.overallMatch >= 40)`.
    - Ensure overallMatch is converted to percentage before comparison (currently line 199 multiplies by 100).
    - Also ensure that on-demand compute (line 118) respects the new threshold (it already uses isSimilar, so just confirm).
  </action>
  <verify>
    API returns only users with watchCount >= 3 and overallMatch >= 40%.
    Test with mock data where some candidates have 0 watchCount or 20% match — they must be excluded.
  </verify>
  <done>
    TwinTasters component receives only qualified similar users.
  </done>
</task>

</tasks>

<verification>
- Unit tests for MIN_MATCH_THRESHOLD usage.
- Integration test: API returns empty array when no candidates meet criteria.
- Manual: Check TwinTasters on page with user having few movies shows appropriate message (handled by component already).
- Lint and test suite pass.
</verification>

<success_criteria>
- All similar users shown have ≥3 completed movies and ≥40% match.
- No users with 0 watch count or low match appear.
- Performance: single groupBy query for enrichment, no N+1.
</success_criteria>

<output>
After completion, create `.planning/phases/27-taste-twins-validation/27-01-SUMMARY.md`
</output>
