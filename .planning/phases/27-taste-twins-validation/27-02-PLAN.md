---
phase: 27
plan: 02
type: execute
wave: 2
depends_on:
  - "27-01"
files_modified:
  - src/lib/taste-map/similarity-storage.ts
  - src/app/api/admin/cleanup/similarity/route.ts
  - src/app/profile/taste-map/TwinTasters.tsx
  - src/app/admin/taste-map/page.tsx
autonomous: false
requirements:
  - VALID-03
user_setup:
  - service: "admin UI"
    why: "Only admin users should see cleanup buttons"
    env_vars:
      - name: "ADMIN_USER_ID"
        source: "Environment variable, already exists"
    dashboard_config: []
must_haves:
  truths:
    - "Orphaned SimilarityScore records (referencing deleted users) are removable via admin action"
    - "Admin can trigger cleanup from /admin/taste-map page"
    - "TwinTasters shows cleanup button for admin users"
  artifacts:
    - path: "src/lib/taste-map/similarity-storage.ts"
      provides: "cleanupOrphanedScores function"
      exports: ["cleanupOrphanedScores"]
    - path: "src/app/api/admin/cleanup/similarity/route.ts"
      provides: "POST endpoint to trigger cleanup"
      exports: ["POST"]
    - path: "src/app/admin/taste-map/page.tsx"
      provides: "Admin page with stats and cleanup buttons"
    - path: "src/app/profile/taste-map/TwinTasters.tsx"
      provides: "Cleanup button visible to admin"
      contains: "button.*cleanup.*orphaned"
  key_links:
    - from: "TwinTasters.tsx"
      to: "/api/admin/cleanup/similarity"
      via: "POST request on button click"
      pattern: "fetch.*/api/admin/cleanup/similarity"
    - from: "admin/taste-map/page.tsx"
      to: "similarity-storage.ts"
      via: "import and call cleanupOrphanedScores"
      pattern: "cleanupOrphanedScores"
---

<objective>
Add cleanup mechanism for orphaned SimilarityScore records and provide admin UI to trigger cleanup and monitor stats.

Purpose: Maintain data hygiene, remove scores for deleted/empty users, give admin control.
Output: Cleanup function, admin API endpoint, admin page, and inline cleanup button in TwinTasters for admins.
</objective>

<execution_context>
@C:/Users/n0rds/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/n0rds/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-taste-twins-validation/RESEARCH.md
@src/lib/taste-map/similarity-storage.ts
@src/app/api/admin/compute-similarities/route.ts
@src/app/profile/taste-map/TwinTasters.tsx
@prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement cleanupOrphanedScores in similarity-storage.ts</name>
  <files>
    src/lib/taste-map/similarity-storage.ts
  </files>
  <action>
    Add function `cleanupOrphanedScores(): Promise<{deleted: number; orphans: string[]}>`.
    Steps:
    - Get all distinct userIdA and userIdB from SimilarityScore using raw SQL or Prisma $queryRaw.
    - Combine into a single set of all user IDs.
    - Fetch existing user IDs from User table (SELECT id).
    - Compute orphaned = allIds - existingIds.
    - Delete all SimilarityScore records where userIdA OR userIdB is in orphaned set.
    - Return deleted count and orphan list.
  </action>
  <verify>
    Function exists, returns object with deleted and orphans. Test with mock DB: records with non-existent userIds get deleted.
  </verify>
  <done>
    Orphaned similarity scores can be removed programmatically.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create admin cleanup API endpoint</name>
  <files>
    src/app/api/admin/cleanup/similarity/route.ts
  </files>
  <action>
    Create new POST endpoint at `/api/admin/cleanup/similarity`.
    - Authenticate admin (same as compute-similarities: check session.user.id === ADMIN_USER_ID).
    - Call `cleanupOrphanedScores()`.
    - Return JSON `{ success: true, deleted: number, orphans: string[] }`.
    - Add rate limiting middleware.
    - Use existing ADMIN_USER_ID from env.
  </action>
  <verify>
    curl -X POST with admin session returns 200 with deleted count.
    Non-admin gets 401.
  </verify>
  <done>
    Admin can trigger cleanup via API.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add TwinTasters cleanup button (admin only)</name>
  <files>
    src/app/profile/taste-map/TwinTasters.tsx
  </files>
  <action>
    Add a small button/icon near the header (next to tooltip) for admins.
    - Fetch current user's admin status via session (already available from server? Need to pass as prop or fetch /api/user/session).
    - If admin, show button with "Очистить кеш близнецов" or a broom icon.
    - On click, POST to `/api/admin/cleanup/similarity?type=orphaned`.
    - Show toast with result: "Удалено X сиротских записей".
    - Disable button during request.
  </action>
  <verify>
    Admin sees button, can click, toast appears.
    Non-admin does not see button.
  </verify>
  <done>
    Quick cleanup accessible from TwinTasters block.
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 4: Create admin taste-map page (UI verification)</name>
  <files>
    src/app/admin/taste-map/page.tsx
  </files>
  <action>
    Create new page `/admin/taste-map` with:
    - Stats: total SimilarityScore count, total unique users (from getSimilarityScoreStats).
    - Buttons:
      * "Очистить сиротские записи" → POST `/api/admin/cleanup/similarity?type=orphaned`
      * "Очистить старые записи (>365 días)" → optional, call deleteOldSimilarityScores
      * "Пересчитать все" → POST `/api/admin/compute-similarities`
    - Simple layout: cards with stats, buttons with loading states, refresh capability.
    - Protect with admin check (redirect if not admin).
  </action>
  <verify>
    Visit /admin/taste-map as admin: page loads, stats displayed, buttons work.
    As non-admin: redirect to / or 401.
  </verify>
  <done>
    Admin has full control over similarity scores.
  </done>
</task>

</tasks>

<verification>
- All tests pass (npm run test:ci).
- Lint clean (npm run lint).
- Admin page accessible only to admin.
- Cleanup API deletes orphaned records correctly.
- TwinTasters button visible only to admin and triggers cleanup.
</verification>

<success_criteria>
- Orphaned SimilarityScore records can be removed.
- Admin interface provides visibility and control.
- TwinTasters block includes convenient cleanup for admins.
</success_criteria>

<output>
After completion, create `.planning/phases/27-taste-twins-validation/27-02-SUMMARY.md`
</output>
