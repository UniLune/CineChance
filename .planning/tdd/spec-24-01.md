# Unit Test Specification: Phase 24 - Taste Map DB Read

## Test Scope

Test the changes to:
1. `src/app/profile/taste-map/page.tsx` - server-side DB fetch
2. `src/app/profile/taste-map/TasteMapClient.tsx` - props-based rendering

## Unit Tests for page.tsx

### Test: fetchPersonProfileData helper
```typescript
describe('fetchPersonProfileData', () => {
  it('should fetch both actor and director profiles in parallel', async () => {
    // Mock prisma.personProfile.findUnique to return data
    // Call fetch logic
    // Expect Promise.all with two calls
  });

  it('should return empty arrays when profiles not found', async () => {
    // Mock prisma to return null
    // Verify fallback to []
  });

  it('should transform data to [name, score] format', async () => {
    // Mock db with PersonData[]
    // Verify output: Array<[string, number]>
  });

  it('should slice to top 10 only', async () => {
    // Mock 20 persons
    // Verify slice(0, 10)
  });

  it('should use correct where clause for actor and director', async () => {
    // Verify findUnique called with:
    // { where: { userId_personType: { userId, personType: 'actor' } } }
    // { where: { userId_personType: { userId, personType: 'director' } } }
  });
});
```

### Test: TasteMapPage component (server)
```typescript
describe('TasteMapPage (server)', () => {
  it('should pass tasteMap, topActors, topDirectors to TasteMapClient', async () => {
    // Mock getServerSession, getTasteMap, prisma
    // Render page
    // Verify TasteMapClient called with correct props
  });

  it('should redirect if no session', async () => {
    // Mock getServerSession to return null
    // Expect redirect('/')
  });

  it('should not compute person profiles (no computeUserPersonProfile call)', async () => {
    // Verify computeUserPersonProfile is NOT imported/called
  });
});
```

## Unit Tests for TasteMapClient

### Test: Props initialization
```typescript
describe('TasteMapClient with props', () => {
  it('should initialize state from props', () => {
    // Render with topActors and topDirectors props
    // Expect component to display them immediately
  });

  it('should not have loadingActors/loadingDirectors states', () => {
    // Verify no useState for loading flags
    // Or states are always false
  });

  it('should not call useEffect for data fetching', () => {
    // Check that component does not use useEffect with fetch
    // Or that fetch functions are not defined
  });
});
```

### Test: Rendering
```typescript
describe('TasteMapClient rendering', () => {
  it('should render actor badges from props', () => {
    // Render with topActors=[['Actor A', 8.5]]
    // Expect to see "Actor A" and "8.5" in output
  });

  it('should render director badges from props', () => {
    // Render with topDirectors=[['Director X', 9.0]]
    // Expect to see "Director X" and "9.0"
  });

  it('should show empty state when props are empty', () => {
    // Render with topActors=[], topDirectors=[]
    // Expect "Нет данных об актерах" / "Нет данных о режиссерах"
  });

  it('should limit to top 10 items', () => {
    // Provide 15 actors, verify only first 10 rendered
  });

  it('should format score to 1 decimal place', () => {
    // Provide score 8.55, expect "8.5" (toFixed(1))
  });

  it('should preserve links to /profile/actors and /profile/creators', () => {
    // Verify href="/profile/actors" in actor badge container
    // Verify href="/profile/creators" in director badge container
  });
});
```

### Test: Error states
```typescript
describe('TasteMapClient error handling', () => {
  it('should handle undefined props gracefully', () => {
    // Render without topActors/topDirectors (should default to [])
    // Expect no crash, empty state
  });

  it('should handle null/undefined score values', () => {
    // Provide [['Actor', 0]] or [['Actor', null as any]]
    // Expect fallback display (0 or '—')
  });
});
```

## Integration Tests

### Test: Full page with Prisma mock
```typescript
describe('TasteMapPage integration', () => {
  it('should render with real PersonProfile data', async () => {
    // Mock prisma.personProfile.findUnique
    // Request page
    // Verify HTML contains actor/director names from mock data
  });
});
```

### Test: Props structure validation
```typescript
it('should have correct prop types', () => {
  // Render with props matching interface
  // TypeScript should catch mismatches
});
```

## Expected Test Results

- All new unit tests: PASS
- All existing tests: PASS (no regressions)
- TypeScript: zero errors

## Test Files to Create/Modify

1. **New**: `src/app/profile/taste-map/__tests__/page.test.tsx` - server tests
2. **New**: `src/app/profile/taste-map/__tests__/TasteMapClient.test.tsx` - client tests
3. **Existing**: Verify no updates needed to global test setup

## Mock Requirements

- `@/lib/prisma` → mock `personProfile.findUnique`
- `next-auth` → mock `getServerSession`
- `next/navigation` → mock `redirect`

## Coverage Goals

- ✅ 100% branch coverage for new data-fetching logic
- ✅ 100% coverage of transformation functions
- ✅ UI rendering paths covered (with data, without data)
- ✅ No reduction in overall project coverage (>80%)
